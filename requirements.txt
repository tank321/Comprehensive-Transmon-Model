# Transmon Qubit Cosmic Ray Impact Simulation
# Requirements for Python 3.7+

# Core scientific computing
numpy>=1.20.0
scipy>=1.7.0
matplotlib>=3.3.0

# Note: All other imports (pickle, os, sys, subprocess, time, shutil,
# datetime, pathlib, dataclasses, typing) are part of Python standard library
